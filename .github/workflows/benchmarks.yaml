name: Benchmarks
on: [push]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  one:
    name: one
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install nightly
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt

      - name: install prerequisites for c2rust
        run: sudo apt install build-essential llvm clang libclang-dev cmake libssl-dev pkg-config python3 git

      - name: install c2rust
        run: cargo install c2rust

      - name: install CrustS
        run: |
          cargo install --path .
          export PATH=$PATH:~/.cargo/bin

      - name: BENCHMARK 1
        working-directory: ./benchmark1
        run: sh benchmark.sh

  two:
    name: two
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install nightly
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt

      - name: install prerequisites for c2rust
        run: sudo apt install build-essential llvm clang libclang-dev cmake libssl-dev pkg-config python3 git

      - name: install c2rust
        run: cargo install c2rust

      - name: install CrustS
        run: |
          cargo install --path .
          export PATH=$PATH:~/.cargo/bin

      - name: BENCHMARK 2
        working-directory: ./benchmark2
        run: sh benchmark.sh
